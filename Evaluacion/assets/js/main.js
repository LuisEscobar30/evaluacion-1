document.addEventListener('DOMContentLoaded', () => {
    console.log("JavaScript vinculado correctamente");
    
    inicializarProyecto();
});

 // Función inicializadora para pruebas de estructura
function inicializarProyecto() {
    
}